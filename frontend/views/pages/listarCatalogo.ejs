    <!DOCTYPE html>
        <html lang="en">
        <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Catálogo de Libros</title>
        <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
        <link href="" rel="stylesheet">
    <style>
        .card {
        margin-bottom: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Sombra */
        transition: box-shadow 0.3s ease; /* Transición suave al pasar el cursor */
        }

        .card:hover {
        
        
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Cambio de sombra al pasar el cursor */
        }

        .card-title {
        font-size: 1.2rem;
        color: #333;
        }

        .card-text {
        color: #666;
        }

        /* Estilos opcionales */
        .container {
        max-width: 900px;
        }

        h1 {
        color: #333;
        text-align: center;
        margin-bottom: 30px;
        }
    </style>
    </head>
    <body>

    <div class="container mt-5">
        <h1 class="text-center mb-4">Catálogo de Libros</h1>
        <div class="row" id="catalogo">
        <!-- Aquí se generarán las cards de libros con JavaScript -->
        </div>
    </div>

    <!-- ... (código previo) -->
<div class="card">
    <div class="card-body">
        <h5 class="card-title">${libro.titulo}</h5>
        <p class="card-text">Autor: ${libro.autor}</p>
        <p class="card-text">Género: ${libro.genero}</p>
        <button class="btn btn-primary agregar-carrito" data-titulo="${libro.titulo}">Agregar al carrito</button>
    </div>
</div>
<!-- ... (código posterior) -->


    <script>


    // Array de objetos con la información de los libros
    const libros = [
    {
        titulo: "El nombre de la rosa",
        autor: "Umberto Eco",
        genero: "Novela histórica"
    },
    {
        titulo: "Cien años de soledad",
        autor: "Gabriel García Márquez",
        genero: "Realismo mágico"
    },
    {
        titulo: "El nombre de la rosa",
        autor: "Umberto Eco",
        genero: "Novela histórica"
    },
    {
        titulo: "Cien años de soledad",
        autor: "Gabriel García Márquez",
        genero: "Realismo mágico"
    },
    {
        titulo: "El nombre de la rosa",
        autor: "Umberto Eco",
        genero: "Novela histórica"
    },
    {
        titulo: "Cien años de soledad",
        autor: "Gabriel García Márquez",
        genero: "Realismo mágico"
    },
    // ... Agregar los otros libros aquí
    ];
let carrito = JSON.parse(localStorage.getItem('carrito')) || [];

// Función para manejar el evento de agregar al carrito
function agregarAlCarrito(event) {
    const titulo = event.target.dataset.titulo;
    const libroSeleccionado = libros.find((libro) => libro.titulo === titulo);

    if (libroSeleccionado) {
    carrito.push(libroSeleccionado);
    

actualizarCarrito();
    console.log(`"${libroSeleccionado.titulo}" ha sido agregado al carrito.`);
    console.log("Contenido del carrito:", carrito);
    }
}

// Función para actualizar el carrito en localStorage
function actualizarCarrito() {
    localStorage.setItem('carrito', JSON.stringify(carrito));
}

// Función para crear las cards de los libros
function crearCards() {
    const catalogo = document.getElementById('catalogo');

    libros.


forEach((libro) => {
    const card = document.createElement('div');
    card.

classList.add('col-md-4');

    card.innerHTML = `
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">${libro.titulo}</h5>
            <p class="card-text">Autor: ${libro.autor}</p>
            <p class="card-text">Género: ${libro.genero}</p>
            <button class="btn btn-primary agregar-carrito" data-titulo="${libro.titulo}">Agregar al carrito</button>
        </div>
    </div>
    `;

    catalogo.

appendChild(card);
    });

  // Agregar evento de clic a los botones "Agregar al carrito"
    const botonesCarrito = document.querySelectorAll('.agregar-carrito');
    botonesCarrito.
    botonesCarrito
forEach((boton) => {
    boton.addEventListener('click', agregarAlCarrito);
    });
}

// Llamar a la función para crear las cards al cargar la página
document.addEventListener('DOMContentLoaded', crearCards);

    



    </script>
    </body>
    </html>
